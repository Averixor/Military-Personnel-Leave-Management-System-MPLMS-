APP_ENV=local

# Dev (default): leave unset or comment out — uses SQLite at ./data/mplms_dev.sqlite3
# DATABASE_URL=sqlite+aiosqlite:///./data/mplms_dev.sqlite3

# Production / integration with Docker PostgreSQL:
# DATABASE_URL=postgresql+asyncpg://mplms:mplms@localhost:5432/mplms

TELEGRAM_BOT_TOKEN=
